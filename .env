# ================================================================
# CommentsSystem Configuration
# ================================================================
# This is a production-ready configuration file
# Copy and modify values as needed for your environment

# ================================================================
# Required Settings
# ================================================================

# PostgreSQL connection string
DB_DSN=postgres://user:password@localhost:5433/postsdb?sslmode=disable

# ================================================================
# Optional Settings (uncomment to override defaults)
# ================================================================

# HTTP server bind address
HTTP_ADDR=:8080

# Storage type (currently only "postgres" is supported)
STORAGE_TYPE=postgres

# Log level: debug, info, warn, error
LOG_LEVEL=info

# ================================================================
# Production Tuning (uncomment to customize)
# ================================================================

# HTTP server timeouts (Go duration format: 15s, 1m, 1h)
# HTTP_READ_TIMEOUT=30s
# HTTP_WRITE_TIMEOUT=30s
# HTTP_IDLE_TIMEOUT=120s
# HTTP_SHUTDOWN_TIMEOUT=30s
# HTTP_REQUEST_TIMEOUT=60s

# Business logic limits
# POSTS_PAGE_LIMIT=20
# COMMENTS_PAGE_LIMIT=50
# MAX_TITLE_LENGTH=500
# MAX_CONTENT_LENGTH=20000
# MAX_COMMENT_LENGTH=5000

# PubSub configuration
# PUBSUB_CHANNEL_BUFFER_SIZE=200
# PUBSUB_KEEP_ALIVE_PING=15s

# CORS configuration (for production, set specific origins)
# CORS_ALLOW_ORIGIN=https://yourdomain.com,https://api.yourdomain.com
# CORS_ALLOW_METHODS=GET, POST, OPTIONS
# CORS_ALLOW_HEADERS=Content-Type, Authorization, X-Requested-With

# GraphQL configuration
# GRAPHQL_PLAYGROUND_TITLE=Production GraphQL API
# GRAPHQL_ENDPOINT=/graphql
# GRAPHQL_ENABLE_INTROSPECTION=false

# ================================================================
# Development/Testing
# ================================================================

# Test database DSN (used for integration tests)
# TEST_DB_DSN=postgres://user:password@localhost:5433/postsdb_test?sslmode=disable
